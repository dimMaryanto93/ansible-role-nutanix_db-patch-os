galaxy_info:
  author: dimmaryanto93
  role_name: ndb_postgres_image
  description: Install docker-compose for linux
  company: PT. Tabeldata Informatika

  issue_tracker_url: https://github.com/dimMaryanto93/ansible-role-ndb-postgres-image/issues

  license: MIT

  min_ansible_version: 2.9

  # If this a Container Enabled role, provide the minimum Ansible Container version.
  # min_ansible_container_version:

  platforms:
  - name: EL
    versions:
    - "7"

  galaxy_tags:
    - nutanix
    - database
    - ndb
    - postgresql

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
  